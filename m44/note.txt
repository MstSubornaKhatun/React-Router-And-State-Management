===============================
44-0 Milestone Overview
===============================

🎯 Cue (কী-ওয়ার্ড / প্রশ্ন)
- আগের milestone এ কী শিখেছি?
- নতুন milestone এ কী শিখবো?
- Phase অনুযায়ী React development এর roadmap কী?
- React Router এর গুরুত্বপূর্ণ hooks কোনগুলো?
- Final project কী হবে?

📝 Notes (বিস্তারিত তথ্য)

1. Previous Milestone Recap
📌 Explanation:
- React এর হাতেখড়ি হয়েছে।
- Simple project বানানো হয়েছে।
- Component তৈরি এবং state management শিখেছি।
- Basic props, event handling, এবং UI rendering শিখেছি।

---

2. Current Milestone Goals
📌 Explanation:
- Complete React development শিখবো।
- Data handling → API থেকে data fetch এবং visualization।
- Routing → বিভিন্ন page এবং sub-page তৈরি।
- User interaction handling → form, events, context API ইত্যাদি।
- Final project → Book Vibe।

---

3. Phase 1: Design and Data Visualization
👉 Tools:
- TailwindCSS → utility-first CSS framework।
- DaisyUI → Tailwind এর উপর ভিত্তি করে UI components।
- Recharts → data visualization library (charts, graphs)।
- Axios → API call করার জন্য।

📌 Explanation:
- এই phase এ UI সুন্দরভাবে design করা শিখবো।
- Data কে chart আকারে visualize করা শিখবো।
- API থেকে data fetch করে UI তে দেখানো শিখবো।

---

4. Phase 2: Routing Mastery
👉 Topics:
- Route create করা।
- Nested route → main page এর ভিতরে sub-page।
- Dynamic route → details page (যেমন: blog/:id)।
- Route level data loader → route এর সাথে data load করা।

👉 Important React Router Hooks:
- useLoaderData() → route loader থেকে data access।
- useParams() → dynamic route parameter access।
- useNavigation() → navigation state track।
- useLocation() → current location/path access।
- useOutletContext() → parent থেকে child route এ context share।

📌 Explanation:
- এই phase এ routing এর সব advanced concept শিখবো।
- Nested এবং dynamic route দিয়ে complex UI তৈরি করা যাবে।
- Hooks দিয়ে route data এবং navigation handle করা যাবে।

---

5. Phase 3: React Core Mastery
👉 Topics:
- Form submission → data input এবং validation।
- Controlled vs Uncontrolled form → state দিয়ে form manage বনাম ref দিয়ে form manage।
- useRef() → DOM element বা mutable value track করার জন্য।
- Custom hook → reusable logic তৈরি করার জন্য।
- Context API → global state share করার জন্য।
- useFormStatus(), useActionState() → form এর status এবং action state track করার জন্য।

📌 Explanation:
- এই phase এ React এর core concepts শিখবো।
- Form handling এবং global state management শিখবো।
- Custom hook দিয়ে code reuse শিখবো।

---

6. Phase 4: Final Project
👉 Project: Book Vibe
📌 Explanation:
- Complete React project বানানো হবে।
- এতে থাকবে:
  - UI design (Tailwind + DaisyUI)
  - Data visualization (Recharts)
  - Routing (React Router)
  - API call (Axios)
  - Form handling এবং Context API
- এই project → milestone এর সব concept একসাথে ব্যবহার করবে।

---

7. Output (Example)
👉 UI:
- সুন্দর design করা app।
- Data visualization charts।
- Multiple pages with nested and dynamic routes।
- Form submission এবং validation।
- Final project → Book Vibe।

---

8. Advantages
- Step by step React mastery।
- Tailwind + DaisyUI → দ্রুত সুন্দর UI।
- Recharts + Axios → data visualization এবং API handling।
- Routing mastery → complex app তৈরি করা যাবে।
- Core mastery → form, context, custom hook।
- Final project → সব concept একসাথে প্রয়োগ।

---

📌 Summary
- আগের milestone → React এর basic শিখেছি।
- নতুন milestone → complete React development শিখবো।
- Phase 1 → design এবং data visualization।
- Phase 2 → routing mastery এবং React Router hooks।
- Phase 3 → React core mastery (form, context, custom hook)।
- Phase 4 → Final project Book Vibe।
- Result → React developer হিসেবে পূর্ণ foundation তৈরি হবে।



===============================
44-1 Setup DaisyUI + Tailwind with Vite-React Application
===============================

🎯 Cue (কী-ওয়ার্ড / প্রশ্ন)
- Vite দিয়ে React project setup কিভাবে করবো?
- TailwindCSS এবং DaisyUI কিভাবে configure করবো?
- Google Fonts (Roboto) কিভাবে যোগ করবো?
- Responsive Navbar এবং Hero section কিভাবে বানাবো?
- প্রতিটি ধাপ কী করছে?

📝 Notes (বিস্তারিত তথ্য)

1. Create Vite Project
👉 Command:
npm create vite@latest  
Project name: tailwind-react-pricing-options  

👉 Next Steps:
cd tailwind-react-pricing-options  
npm install  

📌 Explanation:
- Vite → fast build tool for React apps।
- Project তৈরি হয়েছে `tailwind-react-pricing-options` নামে।
- cd দিয়ে project folder এ প্রবেশ করা হয়েছে।
- npm install → dependencies install করা হয়েছে।

---

2. Install TailwindCSS
👉 Command:
npm install tailwindcss @tailwindcss/vite  

📌 Explanation:
- TailwindCSS → utility-first CSS framework।
- @tailwindcss/vite → Vite plugin integration।
- TailwindCSS install করার পর vite.config.ts এ plugin configure করতে হবে।

👉 vite.config.ts:
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'

export default defineConfig({
  plugins: [react(), tailwindcss()],
})

📌 Explanation (Line by Line):
- import react → React plugin import।
- import tailwindcss → Tailwind plugin import।
- plugins array → React এবং Tailwind plugin যোগ করা হয়েছে।
- ফলে TailwindCSS project এ কাজ করবে।

---

3. Import TailwindCSS
👉 App.css:
@import "tailwindcss";

📌 Explanation:
- TailwindCSS utilities App.css এ import করা হয়েছে।
- এখন Tailwind classes JSX এ ব্যবহার করা যাবে।

---

4. Add Google Font (Roboto)
👉 Code:
@import url('https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,100..900;1,100..900&display=swap');

body {
  font-family: "Roboto", sans-serif;
  font-optical-sizing: auto;
  font-weight: 400;
}

📌 Explanation (Line by Line):
- @import url(...) → Google Fonts থেকে Roboto font import।
- body { font-family: "Roboto", sans-serif; } → পুরো project এ Roboto font ব্যবহার হবে।
- font-optical-sizing: auto → font size automatically adjust হবে।
- font-weight: 400 → default weight normal।

---

5. Install DaisyUI
👉 Command:
npm i -D daisyui@latest  

👉 App.css:
@plugin "daisyui";

📌 Explanation:
- DaisyUI → Tailwind এর উপর ভিত্তি করে তৈরি UI component library।
- npm i -D daisyui@latest → DaisyUI install করা হয়েছে।
- @plugin "daisyui"; → TailwindCSS এ DaisyUI plugin যোগ করা হয়েছে।
- ফলে DaisyUI এর ready-made components ব্যবহার করা যাবে।

---

6. Responsive Navbar (App.jsx)
👉 JSX Code (Conceptual):
<div className="navbar bg-base-100">
  <div className="navbar-start">
    <div className="dropdown">
      <button className="btn btn-ghost lg:hidden">Menu</button>
      <ul className="menu menu-sm dropdown-content mt-3 p-2 shadow bg-base-100 rounded-box w-52">
        <li><a>Home</a></li>
        <li><a>About</a></li>
      </ul>
    </div>
    <a className="btn btn-ghost text-xl">Logo</a>
  </div>
  <div className="navbar-center hidden lg:flex">
    <ul className="menu menu-horizontal px-1">
      <li><a>Home</a></li>
      <li><a>About</a></li>
    </ul>
  </div>
</div>

📌 Explanation (Line by Line):
- <div className="navbar bg-base-100"> → Navbar container।
- navbar-start → left side content।
- dropdown → small screen এ menu dropdown হবে।
- lg:hidden → large screen এ dropdown hide হবে।
- navbar-center hidden lg:flex → large screen এ center menu visible হবে।
- ফলে responsive navbar তৈরি হবে।

---

7. Hero Section with Figure (App.jsx)
👉 JSX Code (Conceptual):
<div className="hero min-h-screen bg-base-200">
  <div className="hero-content flex-col lg:flex-row">
    <img src="image.png" className="max-w-sm rounded-lg shadow-2xl" />
    <div>
      <h1 className="text-5xl font-bold">Welcome to Pricing Options</h1>
      <p className="py-6">Tailwind + DaisyUI Hero section example</p>
      <button className="btn btn-primary">Get Started</button>
    </div>
  </div>
</div>

📌 Explanation (Line by Line):
- hero min-h-screen → full screen hero section।
- hero-content flex-col lg:flex-row → small screen এ column, large screen এ row layout।
- <img ... /> → hero image।
- <h1> → hero title।
- <p> → description।
- <button> → call-to-action button।
- ফলে সুন্দর responsive hero section তৈরি হবে।

---

8. Output (Example)
👉 UI:
- Responsive Navbar → dropdown menu small screen এ, center menu large screen এ।
- Hero Section → image + text + button।
- Tailwind + DaisyUI → সুন্দর design এবং responsive layout।

---

9. Advantages
- Vite → fast React setup।
- TailwindCSS → utility-first styling।
- DaisyUI → ready-made UI components।
- Google Fonts → সুন্দর typography।
- Responsive Navbar + Hero → modern UI design।

---

📌 Summary
- Vite দিয়ে React project setup করা হয়েছে।
- TailwindCSS এবং DaisyUI configure করা হয়েছে।
- Google Fonts (Roboto) যোগ করা হয়েছে।
- Responsive Navbar এবং Hero section তৈরি করা হয়েছে।
- Result → Tailwind + DaisyUI দিয়ে সুন্দর responsive React application।


===============================
44-2 Start a Custom Navigation and Install Lucide Icons
===============================

🎯 Cue (কী-ওয়ার্ড / প্রশ্ন)
- Custom navigation data কীভাবে তৈরি করা যায়?
- NavigationData array of objects আকারে কিভাবে রাখা হয়?
- Lucide icons React এ কিভাবে install এবং ব্যবহার করা যায়?
- প্রতিটি component (App, DaisyNav, NavBar, Link) কী করছে?

📝 Notes (বিস্তারিত তথ্য)

1. Lucide Installation
👉 Command:
npm install lucide-react

📌 Explanation:
- Lucide → modern icon library।
- lucide-react → React এর জন্য Lucide icons।
- Install করার পর import করে JSX এ ব্যবহার করা যাবে।

---

2. App.jsx
👉 Code:
```jsx
import "./App.css";
import DaisyNav from "./components/DaisyNav/DaisyNav";
import NavBar from "./components/NavBar/NavBar";

function App() {
  return (
    <>
      <NavBar></NavBar>
      <DaisyNav></DaisyNav>
      <h1 className="text-7xl underline">Vite + React</h1>
    </>
  );
}

export default App;

📌 Explanation (Line by Line):
 - import "./App.css"; → global CSS import।
 - import DaisyNav, NavBar → custom navigation components import।
 - <NavBar> → custom navigation render।
 - <DaisyNav> → DaisyUI navigation render।
 - <h1> → heading text।
 - export default App; → App component export।


3. DaisyNav.jsx
👉 Code:

const DaisyNav = () => {
  return (
    <div>
      <div className="navbar bg-base-100 shadow-sm">
        <div className="navbar-start">
          <div className="dropdown">
            <div tabIndex={0} role="button" className="btn btn-ghost lg:hidden">
              <svg ... /> {/* hamburger icon */}
            </div>
            <ul className="menu menu-sm dropdown-content ...">
              <li><a>Item 1</a></li>
              <li>
                <a>Parent</a>
                <ul className="p-2">
                  <li><a>Submenu 1</a></li>
                  <li><a>Submenu 2</a></li>
                </ul>
              </li>
              <li><a>Item 3</a></li>
            </ul>
          </div>
          <a className="btn btn-ghost text-xl">daisyUI</a>
        </div>
        <div className="navbar-center hidden lg:flex">
          <ul className="menu menu-horizontal px-1">
            <li><a>Item 1</a></li>
            <li>
              <details>
                <summary>Parent</summary>
                <ul className="p-2 bg-base-100 w-40 z-1">
                  <li><a>Submenu 1</a></li>
                  <li><a>Submenu 2</a></li>
                </ul>
              </details>
            </li>
            <li><a>Item 3</a></li>
          </ul>
        </div>
        <div className="navbar-end">
          <a className="btn">Button</a>
        </div>
      </div>
    </div>
  );
};


📌 Explanation (Line by Line):
 - navbar-start → left side content।
 - dropdown → small screen এ hamburger menu।
 - lg:hidden → large screen এ dropdown hide।
 - navbar-center hidden lg:flex → large screen এ horizontal menu।
 - navbar-end → right side button।
 - DaisyUI classes → responsive navbar তৈরি করছে।


4. NavBar.jsx
👉 Code:

import React from 'react';
import Link from './Link';

const navigationData = [
  { id: 1, name: "Home", path: "/" },
  { id: 2, name: "About", path: "/about" },
  { id: 3, name: "Services", path: "/services" },
  { id: 4, name: "Blog", path: "/blog" },
  { id: 5, name: "Contact", path: "/contact" }
];

const NavBar = () => {
  return (
    <nav>
      <ul className='flex'>
        { navigationData.map(route => <Link route={route}></Link>) }
      </ul>
    </nav>
  );
};

export default NavBar;


📌 Explanation (Line by Line):
 - navigationData → array of objects, প্রতিটি route এর জন্য id, name, path।
 - <ul className='flex'> → Tailwind flex layout।
 - navigationData.map(...) → প্রতিটি route object loop করছে।
 - <Link route={route}> → Link component এ route props পাঠানো হচ্ছে।
 - Result → dynamic navigation তৈরি হচ্ছে।


5. Link.jsx
👉 Code:

const Link = ({route}) => {
  return (
    <li className='px-4'>
      <a href={route.path}>{route.name}</a>
    </li>
  );
};

export default Link;


📌 Explanation (Line by Line):
 - Link component → props থেকে route পাচ্ছে।
 - <li className='px-4'> → padding দিয়ে spacing।
 - <a href={route.path}>{route.name}</a> → route এর path এবং name UI তে দেখাচ্ছে।
 - Result → প্রতিটি navigation link render হচ্ছে।

Output (Example)
👉 UI:
 - Custom NavBar → dynamic links (Home, About, Services, Blog, Contact)।
 - DaisyNav → DaisyUI responsive navbar।
 - Lucide icons → future এ navigation link এর সাথে icon যোগ করা যাবে।

Advantages
 - Dynamic navigation → সহজে update করা যায়।
 - Lucide icons → modern এবং customizable icons।
 - DaisyUI → responsive navbar ready-made।
 - Props → child component এ data পাঠানো যায়।
 - Clear separation → NavBar (list), Link (single item), DaisyNav (UI style)।

📌 Summary
 - navigationData → array of objects দিয়ে dynamic navigation তৈরি হয়েছে।
 - NavBar.jsx  → navigationData loop করে Link component render করছে।
 - Link.jsx  → প্রতিটি route UI তে দেখাচ্ছে।
 - DaisyNav.jsx  → DaisyUI দিয়ে responsive navbar তৈরি হয়েছে।
 - Lucide-react → icon library install করা হয়েছে।
 - Result → custom + DaisyUI navigation, dynamic links, এবং icon support।


===============================
44-3 Dynamic Menu Button Toggle and Menu Toggle
===============================

🎯 Cue (কী-ওয়ার্ড / প্রশ্ন)
- Dynamic menu toggle কীভাবে কাজ করে?
- useState দিয়ে toggle state কিভাবে manage হয়?
- Lucide icons (Menu, X) কীভাবে ব্যবহার করা হয়েছে?
- Responsive design এ md:hidden এবং md:flex এর ভূমিকা কী?
- প্রতিটি line of code কী করছে?

📝 Notes (বিস্তারিত তথ্য)

1. Full Code (NavBar.jsx)
👉 Code:
import React, { useState } from 'react';
import Link from './Link';
import { Menu, X } from 'lucide-react';

const navigationData = [
  { id: 1, name: "Home", path: "/" },
  { id: 2, name: "About", path: "/about" },
  { id: 3, name: "Services", path: "/services" },
  { id: 4, name: "Blog", path: "/blog" },
  { id: 5, name: "Contact", path: "/contact" }
];

const NavBar = () => {
  const [open, setOpen] = useState(false);

  const links = navigationData.map(route => (
    <Link key={route.id} route={route}></Link>
  ));

  return (
    <nav className='flex justify-between mx-10 mt-4'>
      <span className='flex' onClick={() => setOpen(!open)}>
        {open ? (
          <X className='md:hidden'></X>
        ) : (
          <Menu className='md:hidden'></Menu>
        )}
        <ul
          className={`md:hidden absolute duration-1000 text-black
            ${open ? 'top-8' : '-top-40'}
            bg-amber-200`}
        >
          {links}
        </ul>
        <h3 className='ml-4'>My Navbar</h3>
      </span>

      <ul className='md:flex hidden'>
        {links}
      </ul>

      <button>Sign In</button>
    </nav>
  );
};

export default NavBar;

---

2. Link.jsx
👉 Code:
import React from 'react';

const Link = ({ route }) => {
  return (
    <li className='px-4'>
      <a href={route.path}>{route.name}</a>
    </li>
  );
};

export default Link;

---

3. Explanation (Line by Line)
- `import React, { useState }` → React functional component + useState hook।
- `import { Menu, X } from 'lucide-react'` → Lucide icons import।
- `navigationData` → array of objects with id, name, path।
- `const [open, setOpen] = useState(false)` → menu initially closed।
- `const links = navigationData.map(...)` → dynamic links তৈরি হচ্ছে।
- `<span onClick={() => setOpen(!open)}>` → click করলে open state toggle হবে।
- `{ open ? <X> : <Menu> }` → open true হলে X icon, না হলে Menu icon।
- `<ul className={...}>` → mobile view এ menu list দেখাবে, animation সহ।
- `<h3 className='ml-4'>My Navbar</h3>` → navbar title।
- `<ul className='md:flex hidden'> {links} </ul>` → desktop view এ menu items flex আকারে দেখাবে।
- `<button>Sign In</button>` → extra button।

---

4. Responsive Design
📌 Explanation:
- md:hidden → medium screen এ hide হবে।
- md:flex hidden → large screen এ flex layout visible হবে।
- Result → small screen এ toggle menu, large screen এ full menu।

---

5. Output (Example)
👉 Small Screen:
- Menu icon → click করলে open state true হবে।
- X icon → দেখাবে এবং menu list visible হবে।
👉 Large Screen:
- Full navigation links horizontal layout এ দেখাবে।
- Toggle icon hidden থাকবে।

---

6. Advantages
- Dynamic toggle → useState দিয়ে সহজে manage করা যায়।
- Lucide icons → modern এবং customizable icons।
- Responsive design → small এবং large screen এ আলাদা layout।
- Reusable links → navigationData থেকে dynamic link তৈরি।
- Clean separation → Link component আলাদা রাখা হয়েছে।

---

📌 Summary
- navigationData → array of objects দিয়ে dynamic navigation তৈরি হয়েছে।
- useState → menu toggle state manage করছে।
- Lucide icons (Menu, X) → toggle button হিসেবে ব্যবহার হয়েছে।
- Responsive design → small screen এ toggle menu, large screen এ full menu।
- Result → dynamic, responsive, এবং modern navbar।


===============================
44-4 Toggle Menu Items and Create Pricing Data
===============================

🎯 Cue (কী-ওয়ার্ড / প্রশ্ন)
- Toggle menu items কীভাবে কাজ করে?
- useState দিয়ে toggle state কিভাবে manage হয়?
- Lucide icons (Menu, X) কীভাবে ব্যবহার করা হয়েছে?
- Responsive design এ md:hidden এবং md:flex এর ভূমিকা কী?
- Link component এ hover effect কিভাবে যোগ করা হয়েছে?
- Pricing data future এ কিভাবে তৈরি করা যাবে?

📝 Notes (বিস্তারিত তথ্য)

1. Full Code (NavBar.jsx)
👉 Code:
import React, { useState } from 'react';
import Link from './Link';
import { Menu, X } from 'lucide-react';

const navigationData = [
  { id: 1, name: "Home", path: "/" },
  { id: 2, name: "About", path: "/about" },
  { id: 3, name: "Services", path: "/services" },
  { id: 4, name: "Blog", path: "/blog" },
  { id: 5, name: "Contact", path: "/contact" }
];

const NavBar = () => {
  const [open, setOpen] = useState(false);

  const links = navigationData.map(route => <Link key={route.id} route={route}></Link>);

  return (
    <nav className='flex justify-between mx-10'>
      <span className='flex' onClick={() => setOpen(!open)}>
        {
          open ? <X className='md:hidden'></X> : <Menu className='md:hidden'></Menu>
        }
        <ul className={`md:hidden absolute duration-1000 text-black
              ${open ? 'top-8' : '-top-40'}
              bg-amber-200`}>
          {links}
        </ul>
        <h3 className='ml-4'>My Navbar</h3>
      </span>

      <ul className='md:flex hidden'>
        {links}
      </ul>
      <button>Sign In</button>
    </nav>
  );
};

export default NavBar;

---

2. Full Code (Link.jsx)
👉 Code:
import React from 'react';

const Link = ({route}) => {
  return (
    <li className='px-4 lg:mr-10 hover:bg-amber-400'>
      <a href={route.path}>{route.name}</a>
    </li>
  );
};

export default Link;

---

3. Explanation (Line by Line)

📌 NavBar.jsx
- `import React, { useState }` → React functional component + useState hook।
- `import { Menu, X } from 'lucide-react'` → Lucide icons import।
- `navigationData` → array of objects with id, name, path।
- `const [open, setOpen] = useState(false)` → menu initially closed।
- `const links = navigationData.map(...)` → dynamic links তৈরি হচ্ছে।
- `<span onClick={() => setOpen(!open)}>` → click করলে open state toggle হবে।
- `{ open ? <X> : <Menu> }` → open true হলে X icon, না হলে Menu icon।
- `<ul className={...}>` → mobile view এ menu list দেখাবে, animation সহ।
- `${open ? 'top-8' : '-top-40'}` → conditional class, open হলে menu নিচে আসবে, না হলে উপরে hide হবে।
- `<h3 className='ml-4'>My Navbar</h3>` → navbar title।
- `<ul className='md:flex hidden'> {links} </ul>` → desktop view এ menu items flex আকারে দেখাবে।
- `<button>Sign In</button>` → extra button।

📌 Link.jsx
- `Link` component props থেকে route পাচ্ছে।
- `<li className='px-4 lg:mr-10 hover:bg-amber-400'>` → padding, margin, এবং hover effect যোগ করা হয়েছে।
- `<a href={route.path}>{route.name}</a>` → route এর path এবং name UI তে দেখাচ্ছে।
- Result → প্রতিটি navigation link render হচ্ছে, hover করলে background color পরিবর্তন হবে।

---

4. Responsive Design
📌 Explanation:
- md:hidden → medium screen এ hide হবে।
- md:flex hidden → large screen এ flex layout visible হবে।
- Result → small screen এ toggle menu, large screen এ full menu।

---

5. Output (Example)
👉 Small Screen:
- Menu icon → click করলে open state true হবে।
- X icon → দেখাবে এবং menu list visible হবে।
👉 Large Screen:
- Full navigation links horizontal layout এ দেখাবে।
- Toggle icon hidden থাকবে।
👉 Hover Effect:
- Link item এ hover করলে background amber color হবে।

---

6. Advantages
- Dynamic toggle → useState দিয়ে সহজে manage করা যায়।
- Lucide icons → modern এবং customizable icons।
- Responsive design → small এবং large screen এ আলাদা layout।
- Reusable links → navigationData থেকে dynamic link তৈরি।
- Hover effect → UI interaction উন্নত হয়েছে।
- Future scope → pricing data array তৈরি করে একইভাবে dynamic pricing UI বানানো যাবে।

---

📌 Summary
- navigationData → array of objects দিয়ে dynamic navigation তৈরি হয়েছে।
- useState → menu toggle state manage করছে।
- Lucide icons (Menu, X) → toggle button হিসেবে ব্যবহার হয়েছে।
- Responsive design → small screen এ toggle menu, large screen এ full menu।
- Link.jsx → hover effect যোগ করে UI আরও interactive হয়েছে।
- Result → dynamic, responsive, এবং modern navbar, future এ pricing data যোগ করা যাবে।


===============================
44-5 Display Pricing Options in Pricing Card
===============================

🎯 Cue (কী-ওয়ার্ড / প্রশ্ন)
- Pricing data কীভাবে JSON ফাইলে রাখা যায়?
- Suspense এবং use hook দিয়ে data কীভাবে fetch করা হয়?
- PricingOptions component কীভাবে কাজ করে?
- PricingCard component কীভাবে data display করে?
- PricingFeature component এ icon সহ feature list কীভাবে দেখানো হয়?

📝 Notes (বিস্তারিত তথ্য)

1. PricingData.json
👉 Example Data:
[
  {
    "name": "Basic Plan",
    "price": "$29.99/month",
    "id": 1,
    "description": "Ideal for individuals who want to stay fit with essential gym access.",
    "features": [
      "Access to gym facilities during regular hours",
      "Unlimited use of cardio equipment",
      "1 Free personal training session (first month)",
      "Access to basic fitness classes",
      "Free Wi-Fi access"
    ]
  },
  {
    "name": "Premium Plan",
    "price": "$49.99/month",
    "popular": true,
    "id": 2,
    "description": "Perfect for individuals who want additional perks and flexibility.",
    "features": [
      "24/7 gym access",
      "Unlimited use of all gym equipment",
      "3 Free personal training sessions per month",
      "Access to premium fitness classes",
      "Access to sauna and steam room",
      "Discount on merchandise and services",
      "Free towel service"
    ]
  },
  {
    "name": "Family Plan",
    "price": "$99.99/month",
    "id": 3,
    "description": "Great for families who want to stay healthy together.",
    "features": [
      "2 adults and up to 2 children included",
      "24/7 gym access",
      "Unlimited use of gym equipment",
      "Free personal training sessions for each adult",
      "Access to family fitness classes",
      "Free childcare during gym visits",
      "Discounts on family wellness programs",
      "Free access to family wellness events",
      "Exclusive family fitness challenges"
    ]
  }
]

📌 Explanation:
- PricingData.json → array of objects।
- প্রতিটি object এ name, price, id, description, features আছে।
- features → array of strings, প্রতিটি feature আলাদা করে দেখানো হবে।

---

2. PricingOptions.jsx
👉 Full Code:
import React, { use } from 'react';
import PricingCard from '../pricingCard/pricingCard';

const PricingOptions = ({pricingPromise}) => {
  const pricingData = use(pricingPromise);

  console.log(pricingData); // (3) [{…}, {…}, {…}]

  return (
    <div> 
      <h2 className='text-5xl'>Get our Membership</h2>
      <div className='grid md:grid-cols-2 lg:grid-cols-3 gap-8'>
        {
          pricingData.map(pricing => <PricingCard
            key={pricing.id}
            pricing={pricing}></PricingCard>)
        }
      </div>
    </div>
  );
};

export default PricingOptions;

📌 Explanation (Line by Line):
- `use(pricingPromise)` → Suspense এর সাথে কাজ করে promise resolve করে data আনে।
- `console.log(pricingData)` → data console এ দেখা যায়।
- `<h2>` → section heading।
- `<div className='grid ...'>` → responsive grid layout।
- `pricingData.map(...)` → প্রতিটি pricing object loop করছে।
- `<PricingCard pricing={pricing}>` → PricingCard component এ data পাঠানো হচ্ছে।

---

3. PricingCard.jsx
👉 Full Code:
import React from 'react';
import PricingFeature from './PricingFeature';

const PricingCard = ({pricing}) => {
  const {name, price, description, features} = pricing;
  return (
    <div className='border bg-amber-600 rounded-2xl'>
      {/* card header */}
      <div className='grid md:grid-cols-3'>
        <h1 className='text-7xl'>{name}</h1>
        <h4 className='text-3xl'>{price}</h4>
      </div>
      {/* card body */}
      <div className='bg-amber-400 p-4 rounded-2xl mt-10'>
        <p>{description}</p>
        {
          features.map((feature, index) => <PricingFeature key={index} feature={feature}></PricingFeature>)
        }
      </div>
    </div>
  );
};

export default PricingCard;

📌 Explanation (Line by Line):
- `{name, price, description, features}` → destructuring করে pricing object থেকে data নিচ্ছে।
- `<h1>{name}</h1>` → plan name দেখাচ্ছে।
- `<h4>{price}</h4>` → plan price দেখাচ্ছে।
- `<p>{description}</p>` → plan description দেখাচ্ছে।
- `features.map(...)` → প্রতিটি feature loop করে PricingFeature component এ পাঠানো হচ্ছে।

---

4. PricingFeature.jsx
👉 Full Code:
import { CircleCheckBig } from 'lucide-react';
import React from 'react';

const PricingFeature = ({feature}) => {
  return (
    <p className='flex'>
      <CircleCheckBig className='mr-2'></CircleCheckBig>{feature}
    </p>
  );
};

export default PricingFeature;

📌 Explanation (Line by Line):
- `CircleCheckBig` → Lucide icon import করা হয়েছে।
- `<p className='flex'>` → flex layout এ icon + text দেখাচ্ছে।
- `{feature}` → প্রতিটি feature string UI তে দেখাচ্ছে।
- Result → প্রতিটি feature এর পাশে check icon দেখাচ্ছে।

---

✅ Output (Example)
- Membership plans → Basic, Premium, Family।
- প্রতিটি plan → name, price, description, features দেখাচ্ছে।
- Features → check icon সহ list আকারে দেখাচ্ছে।
- Premium plan এ popular flag future এ highlight করা যাবে।

---

✅ Advantages
- JSON data → সহজে update করা যায়।
- Suspense + use → async data fetch সহজ হয়েছে।
- PricingOptions → dynamic loop করে PricingCard তৈরি করছে।
- PricingCard → প্রতিটি plan সুন্দরভাবে UI তে দেখাচ্ছে।
- PricingFeature → icon সহ feature list user-friendly হয়েছে।

---

📌 Summary
- PricingData.json → membership plans data store করছে।
- PricingOptions.jsx → data loop করে PricingCard render করছে।
- PricingCard.jsx → plan details দেখাচ্ছে।
- PricingFeature.jsx → icon সহ feature list দেখাচ্ছে।
- Result → dynamic, responsive, এবং modern pricing section তৈরি হয়েছে।


===============================
44-6 Use Daisy UI Pricing Card to Modify Component
===============================

🎯 Cue (কী-ওয়ার্ড / প্রশ্ন)
- DaisyUI pricing card JSX কীভাবে ব্যবহার করা যায়?
- PricingOptions component এ DaisyPricing কীভাবে integrate করা হয়েছে?
- DaisyPricing component কীভাবে data display করছে?
- Badge, card-body, এবং button class এর ভূমিকা কী?
- প্রতিটি line of code কী করছে?

📝 Notes (বিস্তারিত তথ্য)

1. PricingOptions.jsx
👉 Full Code:
import React, { use } from 'react';
import PricingCard from '../pricingCard/pricingCard';
import DaisyPricing from '../DaisyPricing/DaisyPricing';

const PricingOptions = ({pricingPromise}) => {
  const pricingData = use(pricingPromise);

  return (
    <div> 
      <h2 className='text-5xl'>Get our Membership</h2>
      <div className='grid md:grid-cols-2 lg:grid-cols-3 gap-8'>
        {/* পুরনো PricingCard বাদ দিয়ে DaisyPricing ব্যবহার করা হয়েছে */}
        {
          pricingData.map(pricing => <DaisyPricing key={pricing.id} pricing={pricing}></DaisyPricing>)
        }
      </div>
    </div>
  );
};

export default PricingOptions;

📌 Explanation (Line by Line):
- `import React, { use }` → React এবং Suspense এর জন্য use hook।
- `import DaisyPricing` → নতুন DaisyUI ভিত্তিক pricing card component।
- `const pricingData = use(pricingPromise)` → promise resolve করে pricing data আনা হচ্ছে।
- `<h2>` → section heading।
- `<div className='grid ...'>` → responsive grid layout।
- `pricingData.map(...)` → প্রতিটি pricing object loop করছে।
- `<DaisyPricing pricing={pricing}>` → DaisyPricing component এ data পাঠানো হচ্ছে।

---

2. DaisyPricing.jsx
👉 Full Code:
import React from 'react';

const DaisyPricing = ({pricing}) => {
  const {name, price, features, popular} = pricing;
  return (
    <div className="card w-96 bg-base-100 shadow-sm">
      <div className="card-body">
        {
          popular && <span className="badge badge-xs badge-warning">Most Popular</span>
        }
        <div className="flex justify-between">
          <h2 className="text-3xl font-bold">{name}</h2>
          <span className="text-xl">{price}</span>
        </div>
        <ul className="mt-6 flex flex-col gap-2 text-xs">
          {
            features.map((feature, index) =>  
              <li key={index}>
                <svg xmlns="http://www.w3.org/2000/svg" className="size-4 me-2 inline-block text-success" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M5 13l4 4L19 7" />
                </svg>
                <span>{feature}</span>
              </li>)
          }
        </ul>
        <div className="mt-6">
          <button className="btn btn-primary btn-block">Subscribe</button>
        </div>
      </div>
    </div>
  );
};

export default DaisyPricing;

📌 Explanation (Line by Line):
- `const {name, price, features, popular}` → pricing object থেকে data destructuring।
- `<div className="card ...">` → DaisyUI card container।
- `<div className="card-body">` → card এর content body।
- `popular && <span ...>` → যদি popular true হয় তাহলে badge দেখাবে ("Most Popular")।
- `<div className="flex justify-between">` → name এবং price side‑by‑side দেখাচ্ছে।
- `<h2>{name}</h2>` → plan name।
- `<span>{price}</span>` → plan price।
- `<ul className="mt-6 ...">` → features list container।
- `features.map(...)` → প্রতিটি feature loop করে list item তৈরি করছে।
- `<svg ...>` → check icon (tick mark) দেখাচ্ছে।
- `<span>{feature}</span>` → feature text।
- `<button className="btn btn-primary btn-block">Subscribe</button>` → DaisyUI button, full width।

---

✅ Output (Example)
- Membership plans → DaisyUI card format এ দেখাচ্ছে।
- Popular plan → badge সহ highlight হচ্ছে।
- Features → check icon সহ list আকারে দেখাচ্ছে।
- Subscribe button → প্রতিটি card এ CTA (Call to Action)।

---

✅ Advantages
- DaisyUI card → ready‑made design, সুন্দর UI।
- Badge → popular plan highlight করা যায়।
- Responsive grid → বিভিন্ন screen size এ সুন্দরভাবে layout।
- Features → icon সহ list user‑friendly হয়েছে।
- Button → সহজে CTA যোগ করা যায়।

---

📌 Summary
- PricingOptions.jsx → DaisyPricing component ব্যবহার করছে।
- DaisyPricing.jsx → DaisyUI card দিয়ে plan details দেখাচ্ছে।
- Badge → popular plan highlight করছে।
- Features → check icon সহ list আকারে দেখাচ্ছে।
- Result → modern, responsive, DaisyUI styled pricing section।


===============================
44-7 Add Charts to Your React App
===============================

🎯 Cue (কী-ওয়ার্ড / প্রশ্ন)
- React এ chart কীভাবে যোগ করা যায়?
- Recharts লাইব্রেরি কীভাবে install এবং ব্যবহার করা হয়?
- Student data JSON কীভাবে তৈরি করা হয়েছে?
- LineChart component কীভাবে কাজ করে?
- প্রতিটি line of code কী করছে?

📝 Notes (বিস্তারিত তথ্য)

1. Install Recharts
👉 Command:
npm install recharts

📌 Explanation:
- Recharts → React এর জন্য charting library।
- বিভিন্ন chart যেমন LineChart, BarChart, PieChart ইত্যাদি তৈরি করা যায়।
- Tailwind বা DaisyUI এর সাথে সুন্দরভাবে integrate হয়।

---

2. Student Data (resultData)
👉 Full JSON:
const resultData = [
  { "student_id": 1, "name": "Alice", "physics": 85, "chemistry": 90, "math": 88 },
  { "student_id": 2, "name": "Bob", "physics": 92, "chemistry": 85, "math": 91 },
  { "student_id": 3, "name": "Charlie", "physics": 78, "chemistry": 82, "math": 80 },
  { "student_id": 4, "name": "David", "physics": 88, "chemistry": 86, "math": 92 },
  { "student_id": 5, "name": "Eva", "physics": 95, "chemistry": 91, "math": 93 },
  { "student_id": 6, "name": "Frank", "physics": 80, "chemistry": 78, "math": 85 },
  { "student_id": 7, "name": "Grace", "physics": 91, "chemistry": 94, "math": 90 },
  { "student_id": 8, "name": "Hank", "physics": 70, "chemistry": 72, "math": 75 },
  { "student_id": 9, "name": "Ivy", "physics": 77, "chemistry": 80, "math": 78 },
  { "student_id": 10, "name": "Jack", "physics": 89, "chemistry": 87, "math": 90 }
]

📌 Explanation:
- প্রতিটি object এ student_id, name, physics, chemistry, math আছে।
- physics, chemistry, math → আলাদা property, nested নয়।
- এই data chart এ ব্যবহার করা হবে।

---

3. ResultsChart.jsx
👉 Full Code:
import React from 'react';
import { Line, LineChart, XAxis, YAxis } from 'recharts';

const ResultsChart = () => {
  return (
    <div>
      <LineChart width={800} height={500} data={resultData}>
        <XAxis dataKey="name"></XAxis>
        <YAxis></YAxis>
        <Line dataKey="math"></Line>
        <Line dataKey="chemistry" stroke="red"></Line>
      </LineChart>
    </div>
  );
};

export default ResultsChart;

---

4. Explanation (Line by Line)
- `import { Line, LineChart, XAxis, YAxis } from 'recharts'` → Recharts components import করা হয়েছে।
- `<LineChart width={800} height={500} data={resultData}>` → chart container, width/height সেট করা হয়েছে, data হিসেবে resultData দেওয়া হয়েছে।
- `<XAxis dataKey="name">` → X-axis এ student name দেখাবে।
- `<YAxis>` → Y-axis এ marks scale দেখাবে।
- `<Line dataKey="math">` → math marks line chart এ দেখাবে।
- `<Line dataKey="chemistry" stroke="red">` → chemistry marks আলাদা line, red color দিয়ে দেখাবে।
- Result → দুই subject এর marks line chart এ compare করা যাবে।

---

✅ Output (Example)
- X-axis → student names।
- Y-axis → marks (70–100 range)।
- Blue line → math marks।
- Red line → chemistry marks।
- Chart → প্রতিটি student এর subject wise performance দেখাচ্ছে।

---

✅ Advantages
- Recharts → সহজে chart তৈরি করা যায়।
- JSON data → dynamic ভাবে chart এ ব্যবহার করা যায়।
- Multiple Line → একসাথে একাধিক subject compare করা যায়।
- Responsive → width/height customize করা যায়।
- Visual representation → data সহজে বোঝা যায়।

---

📌 Summary
- Recharts install করে React এ chart যোগ করা হয়েছে।
- resultData → student marks JSON data।
- ResultsChart.jsx → LineChart তৈরি করেছে।
- XAxis → student name।
- YAxis → marks।
- Line → math এবং chemistry marks আলাদা line এ দেখাচ্ছে।
- Result → dynamic, responsive chart যা student performance visualize করছে।
